const { Router } = require("express");
const { post_ContactUsEmail } = require("../controllers/emailControllers");
const router = Router();

//router.post("/postContactUsEmail", post_ContactUsEmail);

module.exports = router;
